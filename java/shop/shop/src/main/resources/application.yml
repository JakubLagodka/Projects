spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/shop
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
  jackson:
    serialization:
      fail-on-empty-beans: false
  flyway:
#    enabled: false
    baselineOnMigrate: true
