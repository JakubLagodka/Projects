public class main {
//    napisz metode ktora zmerguje dwie tablice int i jako wynik zwroci jedna tablice z intami rosnaco
    int[] arr3 = {1,3,5};
    int[] arr2 = {2,4,6};
    while (i < a.length && j < b.length) {
        if (a[i] <= b[j]) {
            result[k++] = a[i++];
        } else {
            result[k++] = b[j++];
        }
    }
}
