public class Saving extends Account {

    public Saving(Long accountNumber, AccountType accountType, Long customerNumber, Double accountState) {
        super(accountNumber, accountType, customerNumber, accountState);
    }
}
